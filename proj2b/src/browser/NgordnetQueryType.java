package browser;

/** An enum to represent the type of query.
 *  Default value is HYPONYMS. */
/** 用于表示查询类型的枚举。

 * 默认值为下标。*/
public enum NgordnetQueryType {
    //下位词，祖先
    HYPONYMS,
    ANCESTORS
}
